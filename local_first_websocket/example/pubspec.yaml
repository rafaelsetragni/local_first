name: local_first_websocket_example
description: Example app demonstrating real-time sync with WebSocket strategy.
publish_to: none

resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  local_first:
    path: ../../local_first
  local_first_websocket:
    path: ../
  local_first_sqlite_storage:
    path: ../../local_first_sqlite_storage
  local_first_shared_preferences:
    path: ../../local_first_shared_preferences
  mongo_dart: ^0.10.3
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
