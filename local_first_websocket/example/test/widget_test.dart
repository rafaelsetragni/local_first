import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Example app smoke test', (WidgetTester tester) async {
    // Basic smoke test to ensure the example app can be imported
    expect(true, isTrue);
  });
}
