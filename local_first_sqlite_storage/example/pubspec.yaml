name: local_first_sqlite_counter_example
description: "SQLite-backed LocalFirst counter example."
publish_to: "none" #
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  local_first:
  local_first_sqlite_storage:
  local_first_websocket:
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
