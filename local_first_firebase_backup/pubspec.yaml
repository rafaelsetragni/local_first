name: local_first_firebase_backup
description: "Firebase Storage backup provider for local_first: cross-platform backup and restore using Firebase Cloud Storage."
version: 0.1.0
repository: https://github.com/rafaelsetragni/local_first
issue_tracker: https://github.com/rafaelsetragni/local_first/issues

resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: ">=1.17.0"

topics:
  - local-first
  - firebase
  - backup
  - cloud-storage

dependencies:
  flutter:
    sdk: flutter
  local_first: ^0.7.0
  firebase_storage: ^12.3.7
  firebase_auth: ^5.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.3
