name: local_first_gdrive_backup
description: "Google Drive backup provider for local_first: backup and restore app data using Google Drive App Data folder."
version: 0.1.0
repository: https://github.com/rafaelsetragni/local_first
issue_tracker: https://github.com/rafaelsetragni/local_first/issues

resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: ">=1.17.0"

topics:
  - local-first
  - google-drive
  - backup
  - cloud-storage

dependencies:
  flutter:
    sdk: flutter
  local_first: ^0.8.0
  googleapis: ^13.2.0
  google_sign_in: ^6.2.2
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.3
