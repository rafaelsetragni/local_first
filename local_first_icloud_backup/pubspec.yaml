name: local_first_icloud_backup
description: "iCloud backup provider for local_first: backup and restore app data using iCloud Documents on iOS/macOS."
version: 0.1.0
repository: https://github.com/rafaelsetragni/local_first
issue_tracker: https://github.com/rafaelsetragni/local_first/issues

resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: ">=1.17.0"

topics:
  - local-first
  - icloud
  - backup
  - cloud-storage

dependencies:
  flutter:
    sdk: flutter
  local_first: ^0.7.0
  icloud_storage: ^2.1.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.3
