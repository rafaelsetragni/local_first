name: local_first_hive_storage
description: "Hive adapter for local_first: schema-less, offline-first storage with Hive boxes and metadata support."
version: 0.1.0
repository: https://github.com/rafaelsetragni/local_first
issue_tracker: https://github.com/rafaelsetragni/local_first/issues

resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: ">=1.17.0"

topics:
  - local-first
  - hive
  - storage

dependencies:
  flutter:
    sdk: flutter
  local_first: ^0.5.0
  hive_ce: ^2.16.0
  hive_ce_flutter: ^2.3.3
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.3
